# Copy this file to `.env.local` and fill in your project-specific values.
# Do NOT commit `.env.local` with real secrets to version control.

VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-publishable-key

# Optional (only for advanced local testing). Do NOT expose this in the browser.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# NOTE: The `LOVABLE_API_KEY` must be set as a secret in your Supabase Functions environment
# (via the Supabase Dashboard or CLI). Do NOT put the AI key in the frontend env.
# Example (set in Supabase Dashboard -> Functions -> Environment variables / Secrets):
# LOVABLE_API_KEY=sk_... 
